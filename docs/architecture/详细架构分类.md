●现在我对整个项目有了完整理解，下面给出详细的架构分类。

---
  项目整体架构分类

  📁 顶层文件

  little_term-master/
  │
  ├── app_launcher.py           # 应用启动器（含后台线程管理）
  ├── config.py                 # 全局配置文件（DB、LLM、路径等）
  ├── knowledge_base/           # 知识库数据目录（JSON文件）
  ├── data/                     # 运行时数据目录（抓包临时文件等）
  ├── static/                   # 生产模式SPA静态资源
  │
  ├── xiaoxueqi/                # 🔵 Flask Web后端（蓝图路由层）
  ├── frontend/                 # 🟢 Vue3前端（SPA）
  └── utils/                    # 🟡 核心业务逻辑层

---
  🔵 xiaoxueqi/ —Flask Web后端（路由+视图层）

  ▎ 职责：对外提供REST API，对内调度各业务模块。是前端与核心引擎之间的桥梁。

  xiaoxueqi/
  ├── __init__.py                    # Flask工厂函数 create_app()，注册所有蓝图
  ├── views/
  │   ├── main.py                    # 根路由 /（首页、SPA入口）
  │   ├── dashboard.py               # /dashboard —仪表盘API（统计、摘要）
  │   ├── detection_view.py          # /detection —检测模块API（触发检测、查询告警）
  │   ├── attack_view.py             # /attack —攻击捕获API（证据、攻击链）
  │   ├── attribution_view.py        # /attribution —溯源API（归因、画像、报告）
  │   ├── traceback_view.py          # /traceback —攻击链追溯API（图数据）
  │   ├── scenario_view.py           # /scenario —场景管理API（启动/停止/状态）
  │   ├── log_view.py                # /logs + /api —主机日志API（LogonTracer）
  │   ├── behavior_view.py           # /behavior —主机行为API（Sysmon/进程）
  │   └── traffic_view.py            # /traffic —网络流量API（抓包/分析）

---
  🟢 frontend/src/ —Vue3前端（SPA展示层）

  ▎ 职责：可视化展示检测告警、攻击链、溯源报告、网络拓扑等。

  frontend/src/
  ├── App.vue                        # 根组件
  ├── main.ts                        # Vue应用入口
  ├── router/index.ts                # 前端路由定义
  ├── stores/app.ts                  # 全局状态管理（Pinia）
  ├── utils/time.ts                  # 时间格式化工具
  │
  ├── api/                           # 📡 后端API调用封装
  │   ├── client.ts                  #   Axios实例（baseURL、拦截器）
  │   ├── dashboard.ts               #   仪表盘数据API
  │   ├── detection.ts               #   检测结果API
  │   ├── attack.ts                  #   攻击捕获API
  │   ├── attribution.ts             #   溯源归因API
  │   ├── scenario.ts                #   场景管理API
  │   ├── traceback.ts               #   攻击追溯API
  │   ├── logs.ts                    #   主机日志API
  │   ├── behavior.ts                #   主机行为API
  │   └── traffic.ts                 #   网络流量API
  │
  └── views/                         # 🖥️页面视图组件
      ├── DashboardView.vue          #   仪表盘总览
      ├── DetectionView.vue          #   检测告警列表
      ├── AttackChainView.vue        #   攻击链可视化
      ├── AttributionView.vue        #   溯源分析结果
      ├── TracebackView.vue          #   攻击追溯图
      ├── ScenarioView.vue           #   场景管理面板
      ├── LogsView.vue               #   主机日志浏览
      ├── LogDetailView.vue          #   日志详情
      ├── LogontracerView.vue        #   LogonTracer登录追踪
      ├── BehaviorView.vue           #   主机行为监控
      ├── TrafficView.vue            #   网络流量列表
      ├── TrafficDetailView.vue      #   流量详情
      ├── NetworkTopologyView.vue    #   网络拓扑图
      └── HomeView.vue               #   首页/导航

---
  🟡 utils/ —核心业务逻辑层（按功能分为9个子系统）

  1. utils/detection/ —🎯 攻击行为检测模块

  ▎ 对应实验要求：模块一（Detection）。多源数据接入→异常检测→威胁建模→告警分级。

  utils/detection/
  ├── __init__.py
  ├── llm_detector.py             # 🔥 核心：LLM增强攻击检测引擎
  │   ├── _rule_based_detect()    #    规则匹配（9条规则: 扫描/WMI/隐蔽信道/AI/凭据...）
  │   ├── _rag_enhanced_detect()  #    RAG知识库增强检测
  │   ├── _llm_analyze()          #    LLM深度分析（调用DeepSeek做JSON格式安全研判）
  │   ├── _classify_severity()    #    告警分级（high/medium/low）
  │   └── _deduplicate_alerts()   #    告警去重
  │
  └── rag_knowledge_base.py       # 🔥 核心：RAG检索增强知识库
      ├── 加载3类知识源            #    (1)rag_corpus.json (2)attck_techniques.json (3)apt_groups.json
      ├── _tokenize()              #    中英文混合分词
      ├── _build_index()           #    TF-IDF向量索引构建
      ├── search()                 #    余弦相似度语义检索
      ├── get_attck_context()      #    ATT&CK技术上下文查询
      └── get_apt_context()        #    APT组织上下文查询

  2. utils/capture/ —🔗 攻击行为捕获模块

  ▎ 对应实验要求：模块二（Capture）。多智能体协作→攻击链重构→证据固化。

  utils/capture/
  ├── __init__.py
  ├── agent_framework.py          # 🔥 核心：多智能体协作捕获框架（AgentStalker风格）
  │   ├── EvidenceCollectorAgent  #    Agent1: 证据采集（从原始事件中提取关键证据）
  │   ├── ChainBuilderAgent       #    Agent2: 攻击链构建（按Kill Chain阶段排序）
  │   ├── VerifierAgent           #    Agent3: 验证（IP连通性/时间顺序/战术逻辑校验）
  │   ├── ArbitratorAgent         #    Agent4: 裁决（LLM综合研判 + IOC提取 + 处置建议）
  │   └── MultiAgentCaptureFramework  # 4 Agent编排器（串行流水线）
  │
  ├── provenance_tracker.py       # 🔥 核心：系统溯源数据追踪器
  │   ├── ProvenanceNode          #    溯源图节点（Process/File/IP/User四类）
  │   ├── ProvenanceEdge          #    溯源图边（Fork/Connect/Logon等关系）
  │   ├── add_process/file/       #    节点添加API
  │   │   network/user_logon()
  │   ├── get_causal_chain()      #    因果链提取（BFS遍历）
  │   ├── get_full_graph()        #    全量溯源图导出
  │   └── reconstruct_attack_path #    攻击路径反向追溯
  │
  └── evidence_chain.py           # 🔥 核心：证据链固化（类区块链结构）
      ├── EvidenceBlock           #    证据块（SHA-256哈希 + 前驱指针）
      ├── EvidenceChain           #    证据链（add→seal→verify）
      ├── EvidencePackage         #    证据包（多链管理 + ISO 27037标准 + JSON导出）
      ├── seal()                  #    封存（追加封存块）
      └── verify()                #    验证（遍历检查哈希一致性）

  3. utils/attribution/ —🎭 攻击溯源模块

  ▎ 对应实验要求：模块三（Attribution）。威胁情报融合→证据路径推理→攻击者画像→报告生成。

  utils/attribution/
  ├── __init__.py
  ├── attribution_engine.py       # 🔥 核心：攻击溯源归因引擎（TAA-EPLMR风格）
  │   ├── _extract_ttps()         #    TTP提取（从攻击链中抽取技术ID）
  │   ├── _match_ttps_to_apt()    #    Jaccard相似度匹配已知APT组织
  │   ├── _analyze_behavior()     #    行为画像分析（技能/复杂度/OPSEC）
  │   ├── _analyze_infrastructure #    基础设施分析（C2节点/内外网IP）
  │   ├── _synthesize_attribution #    综合归因（多维度融合判决）
  │   ├── _build_evidence_path()  #    证据路径推理链构建
  │   └── generate_attribution_report()  # 自动生成Markdown归因报告
  │
  ├── attacker_profiler.py        # 🔥 核心：攻击者综合画像生成器
  │   ├── build_profile()         #    构建5维画像：
  │   │   ├── identity            #      身份特征（组织/国家/别名）
  │   │   ├── capability          #      能力评估（技能/工具/战术范围/AI能力）
  │   │   ├── motivation          #      动机分析（目标/行业/影响）
  │   │   ├── behavioral_patterns #      行为模式（入口/横向/C2/外传偏好）
  │   │   ├── ioc_profile         #      IOC画像（基础设施类型/哈希/域名）
  │   │   └── threat_rating       #      威胁等级评分（0-10分制）
  │   └── _calculate_threat_rating()  # 威胁等级计算（技能+复杂度加权）
  │
  └── report_generator.py         # 🔥 核心：结构化溯源分析报告生成器
      ├── generate_comprehensive_report()  # 生成8章节完整报告
      │   ├── 一、执行摘要
      │   ├── 二、攻击行为检测结果
      │   ├── 三、攻击行为捕获结果（攻击链详情）
      │   ├── 四、攻击溯源归因分析
      │   ├── 五、攻击者综合画像
      │   ├── 六、处置建议（立即/短期/长期）
      │   ├── 七、IOC清单
      │   └── 八、分析方法论
      └── export_visualization_data()     # 导出前端可视化数据（节点/边/雷达图）

  4. utils/scenarios/ —🎬 攻击场景模拟子系统

  ▎ 对应实验要求：3个分析场景。可独立运行的攻击事件生成器。

  utils/scenarios/
  ├── __init__.py
  ├── scenario_base.py             # 攻击场景抽象基类
  │   ├── AttackScenario(ABC)      #   定义接口：generate_attack_sequence()
  │   ├── start()/stop()           #   线程管理（后台运行）
  │   ├── _run_sequence()          #   按阶段顺序生成事件
  │   ├── on_event()               #   事件回调注册
  │   └── _make_event()            #   事件工厂方法
  │
  ├── apt_full_chain.py            # 🎯 场景一：APT全链条攻击
  │   └── APTFullChainScenario     #   Lazarus Group风格，8个阶段：
  │       ├── 阶段0: 侦察（端口扫描）
  │       ├── 阶段1: 初始入侵（Web漏洞→下载dropper）
  │       ├── 阶段2: 执行与持久化（Python payload→Webshell→Crontab）
  │       ├── 阶段3: 内网侦察（whoami/ifconfig/netstat/端口扫描）
  │       ├── 阶段4: 凭据窃取（/etc/shadow读取→SSH暴力破解）
  │       ├── 阶段5: 横向移动（SSH→WMI攻击域控）
  │       ├── 阶段6: C2通信（DNS隧道+ICMP隧道）
  │       ├── 阶段7: 数据外传（打包敏感数据→HTTPS外传）
  │       └── 阶段8: 痕迹清除（清日志→删文件→清history）
  │       网络区域：45.33.22.11(外部C2) →192.168.86.10(DMZ) →192.168.86.130(内网) →192.168.86.131(域控)
  │
  ├── ai_agent_abuse.py            # 🤖 场景二：AI智能体滥用攻击
  │   └── AIAgentAbuseScenario     #   Claude Code/OpenCode滥用，6个阶段：
  │       ├── 阶段1: AI Agent接入与侦察（claude→whoami→find→AI代码生成端口扫描器）
  │       ├── 阶段2: AI生成攻击载荷（反弹Shell→提权脚本→试错循环）
  │       ├── 阶段3: Agent工具链滥用（curl/wget/python→base64解码→stage2执行）
  │       ├── 阶段4: 横向渗透（SSH→WMI→SMB多路径尝试→路径切换）
  │       ├── 阶段5: AI供应链攻击（模型注册表探测→模型后门→投毒上传）
  │       └── 阶段6: 数据外传与清理
  │       特征标签：ai_pattern, llm_generated, trial_and_error, path_switching,
  │                supply_chain_attack, tool_chain, model_poisoning
  │
  ├── ransomware_scenario.py       # 💰 场景三：勒索软件攻击
  │   └── RansomwareScenario      #   Conti/LockBit风格，6个阶段：
  │       ├── 阶段1: 初始入侵（钓鱼文档→宏→PowerShell下载）
  │       ├── 阶段2: 执行与持久化（rundll32→VSS删除→注册表持久化）
  │       ├── 阶段3: 凭据窃取（LSASS dump→SAM/SYSTEM注册表导出）
  │       ├── 阶段4: 内网发现（net view→nltest→端口扫描）
  │       ├── 阶段5: 横向移动+数据外传（SMB→文件服务器→ntdsutiNTDS提取）
  │       └── 阶段6: 影响（加密文件→勒索信→VSS删除→恢复禁用）
  │       网络区域：203.0.113.50(C2) →172.16.50.100(终端) →172.16.50.10(文件服务器)
  │
  └── scenario_manager.py          # 场景管理器
      ├── 注册3个默认场景
      ├── start/stop/status        #   场景生命周期管理
      ├── _on_event()              #   事件自动桥接到DataBridge统一数据层
      ├── add_alert/get_alerts()   #   告警管理
      └── get_detection_stats()    #   检测统计（按严重度/战术/技术聚合）

  5. utils/network_env/ —🌐 企业网络环境模拟器

  ▎ 对应实验要求：内网环境搭建。≥5台计算机、DMZ+内网2层网络、高低安全区域。

  utils/network_env/
  ├── __init__.py
  └── network_env.py               # 企业网络拓扑模拟
      ├── NETWORK_TOPOLOGY         #   静态拓扑定义
      │   ├── zones (5个安全域)    #     external(外部) / dmz(低安全) / internal(中安全)
      │   │                       #     secure_internal(高安全) / management(SOC管理区)
      │   ├── nodes (6个节点)      #     C2服务器 / DMZ-Web / 内网Linux / 域控 /
      │   │                       #     SOC分析平台 / 员工工作站（跳板机）
      │   └── network_routes       #     网络路由规则（允许/阻断策略）
      │
      ├── NetworkNode              #   节点类（状态/CPU/内存/安全事件/连接数）
      │   ├── compromise()         #      标记已攻陷
      │   ├── isolate()            #      隔离节点
      │   └── restore()            #      恢复节点
      │
      ├── NetworkEnvironment       #   网络环境管理器
      │   ├── get_topology()       #      获取完整拓扑（含攻陷/隔离/运行统计）
      │   ├── get_security_posture #      安全态势评估（按区域聚合风险）
      │   ├── generate_traffic_log #      生成模拟流量日志
      │   ├── generate_host_log()  #      生成模拟主机日志（登录/注销/清日志）
      │   ├── generate_behavior_log#      生成模拟主机行为（进程/文件/注册表）
      │   └── start_monitor()      #      后台监控（CPU/内存波动模拟）
      └── get_network_env()        #   全局单例访问

  6. utils/trace/ —🔍 攻击链追溯与分析管线

  ▎ 职责：生产级分析管线。从SQL Server/Neo4j读取真实数据，做ATT&CK映射→图谱构建→APT分析→报告生成。

  utils/trace/
  ├── main_pipeline.py             # 🔥 核心：主分析管线（后台循环任务）
  │   ├── _run_with_sql_server()   #    生产模式：SQL Server + Neo4j
  │   ├── _run_with_databridge()   #    降级模式：DataBridge内存数据
  │   ├── multi_source_llm_analysis#    多源LLM分析（HostLogs+Behaviors+Traffic）
  │   ├── run_ingestion_cycle()    #    单次采集→分析循环
  │   └── pipeline_loop()          #    持续后台分析（每60秒轮询）
  │
  ├── DB_Connector.py              # 数据库连接器（SQL Server读写 + 增量查询）
  ├── Graph_construct.py           # Neo4j图谱构建引擎（节点+关系+因果链）
  ├── Graph_Serializer.py          # 图谱序列化（Neo4j→JSON/D3.js格式）
  ├── Attck_Map.py                 # ATT&CK映射引擎（YAML规则→事件→技术ID）
  ├── APT_Analysis_Engine.py       # APT分析引擎（图谱遍历+攻击链提取）
  ├── CTI_Loader.py                # 威胁情报加载器
  ├── Script_CTI_Loader.py         # CTI加载脚本
  ├── Threat_Intel.py              # 威胁情报数据结构
  ├── LLM_Reporter.py              # LLM报告生成
  ├── StateManager.py              # 状态管理器（检查点 + PID缓存持久化）
  ├── app2.py                      # 辅助应用入口
  │
  └── service/                     # 服务层
      ├── attack_reports_store.py  #   攻击报告持久化存储
      ├── scenario_linker.py       #   场景关联（AttackEvent→ScenarioID映射）
      └── traceback_service.py     #   追溯服务（提供API查询接口）

  7. utils/traffic_fenxi/ —📡 网络流量采集与分析子系统

  ▎ 对应：多源数据接入之"网络流量日志"。

  utils/traffic_fenxi/
  ├── __init__.py
  ├── live_capture.py              # 实时抓包（Scapy/tshark）
  ├── live_capture_dumpcap.py      # 实时抓包（Wireshark dumpcap）
  ├── ingest_offline.py            # 离线PCAP文件导入
  ├── ingest_stream.py             # 流式流量接入
  ├── parser.py                    # 流量解析器（pcap→结构化数据）
  ├── storage_sqlserver.py         # SQL Server流量存储层
  │
  └── analyzer/                    # 流量分析引擎
      ├── __init__.py
      ├── anomaly_detector.py      #   异常协议行为检测（端口/频率/协议异常）
      ├── covert_channel_detector.py  # 隐蔽信道检测（DNS隧道/HTTP隧道/ICMP隧道）
      └── session_rebuild.py       #   会话重建（TCP流重组）

  8. utils/winlog/ —📋 Windows事件日志采集子系统

  ▎ 对应：多源数据接入之"系统日志（Windows Event Log）"。

  utils/winlog/
  ├── __init__.py
  ├── collector_windows.py         # Windows Event Log采集器（EvtSubscribe→XML解析）
  ├── parser_winlogbeat.py         # WinlogBeat输出解析器
  ├── artifacts.py                 # 事件产物提取（进程/文件/注册表/网络）
  ├── session_rebuild.py           # 登录会话重建（Logon→Logoff配对）
  ├── state_store.py               # 采集状态持久化（断点续传）
  ├── time_sync.py                 # 时间同步（跨主机时间对齐）
  │
  ├── storage/
  │   └── hostlogs_sqlserver.py    #   SQL Server日志存储
  │
  ├── service/                     # LogonTracer登录追踪服务
  │   ├── hostlogs_ingest.py       #   日志入站处理
  │   ├── logontracer_service.py   #   登录追踪主服务
  │   └── logontracer_jobs.py      #   定时任务（会话分析/异常检测）
  │
  └── legacy/                      # 历史兼容模块
      ├── __init__.py
      ├── storage_sqlite.py        #   SQLite存储（旧版）
      └── winlogbeat_config.py     #   WinlogBeat配置生成

  9. utils/behavior_monitor/ —🖥️主机行为监控子系统

  ▎ 对应：多源数据接入之"终端行为数据"。

  utils/behavior_monitor/
  ├── __init__.py
  ├── host_monitor_windows.py      # Windows Sysmon监控（进程创建/文件/注册表/网络）
  ├── host_monitor_linux.py        # Linux Auditd监控（进程/文件/网络）
  ├── client_agent.py              # 轻量级采集客户端（部署到各主机）
  ├── xdr_launcher.py              # XDR启动器（一键部署采集器）
  │
  ├── service/
  │   ├── behavior_manager.py      #   行为数据管理服务
  │   └── hostbehaviors_ingest.py  #   行为数据入站处理
  │
  └── storage/
      └── hostbehaviors_sqlserver.py  # SQL Server行为数据存储

  10. 支撑模块

  utils/
  ├── __init__.py
  ├── data_bridge.py               # 🔥 统一数据访问桥接器
  │   ├── DataBridge (单例)        #    生产模式(SQL Server) ↔模拟模式(内存) 自动降级
  │   ├── insert/query/count       #    CRUD操作
  │   ├── get_all_events()         #    跨源事件聚合
  │   └── clear_all()              #    数据清理
  │
  ├── db/db.py                     # 数据库底层操作（pyodbc连接池）
  ├── dashboard/                   # 仪表盘业务层
  │   ├── __init__.py
  │   ├── dashboard_service.py     #   统计聚合+容错降级
  │   └── dashboard_storage.py     #   SQL Server仪表盘数据读取
  │
  └── demo_attack_script.py        # 演示用攻击脚本

---
  🏗️系统架构全景图

  ┌──────────────────────────────────────────────────────────────────┐
  │                       🟢 前端展示层 (Vue3)                        │
  │  Dashboard │ Detection │ AttackChain │ Attribution │ Scenario    │
  │  NetworkTopology │ Logs │ Behavior │ Traffic │ Traceback         │
  └──────────────────────────┬───────────────────────────────────────┘
                             │ REST API (Flask CORS)
  ┌──────────────────────────▼───────────────────────────────────────┐
  │                     🔵 Web后端路由层 (Flask)                       │
  │  /dashboard │ /detection │ /attack │ /attribution │ /scenario    │
  │  /logs │ /behavior │ /traffic │ /traceback                      │
  └──────────────────────────┬───────────────────────────────────────┘
                             │
  ┌──────────────────────────▼───────────────────────────────────────┐
  │                    🟡 核心业务逻辑层 (utils/)                       │
  │                                                                   │
  │  ┌─────────────┐  ┌─────────────┐  ┌──────────────┐              │
  │  │ ①detection │  │ ②capture   │  │ ③attribution│              │
  │  │ LLM检测+RAG │  │ 4Agent协作  │  │ TTP匹配+画像 │              │
  │  │ 规则+语义   │  │ 溯源图+证据链│  │ 归因+报告    │              │
  │  └──────┬──────┘  └──────┬──────┘  └──────┬───────┘              │
  │         │                │                │                       │
  │         └────────────────┼────────────────┘                       │
  │                          │                                        │
  │  ┌───────────────────────┼────────────────────────────┐          │
  │  │          ④scenarios (攻击场景模拟)                  │          │
  │  │    APT全链条 │ AI Agent滥用 │ 勒索软件              │          │
  │  └───────────────────────┬────────────────────────────┘          │
  │                          │                                        │
  │  ┌───────────────────────┼────────────────────────────┐          │
  │  │  ⑤network_env        │  ⑥trace (分析管线)        │          │
  │  │  6节点/5安全区域       │  Neo4j图+ATT&CK映射+LLM    │          │
  │  └───────────────────────┴────────────────────────────┘          │
  │                                                                   │
  │  ┌────────────────┐ ┌──────────────┐ ┌──────────────────┐        │
  │  │ ⑦traffic_fenxi│ │ ⑧winlog     │ │ ⑨behavior_monitor│       │
  │  │ 网络流量采集    │ │ Windows日志  │ │ 主机行为监控     │        │
  │  │ 隐蔽信道检测    │ │ LogonTracer  │ │ Sysmon/Auditd   │        │
  │  └────────────────┘ └──────────────┘ └──────────────────┘        │
  │                                                                   │
  │  ┌──────────────────────────────────────────────────────┐        │
  │  │              ⑩支撑模块                              │        │
  │  │  DataBridge (统一数据桥) │ db.py (数据库) │ dashboard │        │
  │  └──────────────────────────────────────────────────────┘        │
  └──────────────────────────────────────────────────────────────────┘
                             │
  ┌──────────────────────────▼───────────────────────────────────────┐
  │                     🗄️数据持久化层                                │
  │  SQL Server (SecurityTraceDB) │ Neo4j (图数据库) │ JSON文件      │
  │  HostLogs │ HostBehaviors │ NetworkTraffic │ AttackReports       │
  │  Alerts │ DetectionDetails │ AnalysisReports                     │
  └──────────────────────────────────────────────────────────────────┘

---
  📊 功能与实验要求对照

  ![6dbe05c5-f6d3-467f-ba5d-6cb7e7593d47](E:\Tencent\wechatchat\xwechat_files\wxid_9pnt7aagjdpq22_3f82\temp\InputTemp\6dbe05c5-f6d3-467f-ba5d-6cb7e7593d47.png)